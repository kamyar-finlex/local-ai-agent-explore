# Build the example record service

Implement the service described in the project README: an in-memory record
store, a routing layer over it, and the assembly that wires the two together so
that importing the package yields a usable app.

The whole thing must be installable and testable with pytest, with no dependency
the README does not already sanction.
